#!/usr/bin/env bash

while read -r line; do
	case "$line" in
		*"[on]"*) state=Unmuted; break ;;
		*"[off]"*) state=Muted; break ;;
	esac
done < <(amixer set Capture toggle)

notify-send -r 1 -t 2000 "$state"
