#!/usr/bin/env bash

BEFORE=$(playerctl status 2>/dev/null)
[ -z "$BEFORE" ] && exit # no players found

playerctl play-pause
dunstify -r 9990 -t 2000 "$([ "$BEFORE" = "Playing" ] && echo Paused || echo Playing)"
